alert("페이지 추가해주시옵소서");
$('.boards').append(`
  <%= @boards.each do |board|%>
    <tr class="board" data-id=" <%= board.id %> ">
      <td><%= board.id %></td>
      <td class="title"><%= board.title %></td>
    </tr>
  <% end %>
  `)
